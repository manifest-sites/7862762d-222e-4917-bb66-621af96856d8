{
  "name": "HouseholdMember",
  "type": "object",
  "properties": {
    "organizationId": {
      "type": "string",
      "description": "ID of the organization"
    },
    "householdId": {
      "type": "string",
      "description": "ID of the household"
    },
    "personId": {
      "type": "string",
      "description": "ID of the person"
    },
    "relationship": {
      "type": "string",
      "enum": ["head", "spouse", "child", "other"],
      "description": "Relationship within the household"
    }
  },
  "required": ["organizationId", "householdId", "personId", "relationship"]
}